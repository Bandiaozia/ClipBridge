# 架构

客户端使用 REST 完成账户、设备和配对管理，通过单条 WSS 长连接交换端到端加密消息。
中继以 `(recipient_device_id, message_id)` 唯一约束去重，ACK 后立即删除暂存密文。

阶段边界：

1. 基础仓库与可启动入口。
2. 认证、设备、配对、中继、离线队列和部署。
3. 桌面端完整同步。
4. Android 完整同步。
5. 客户端端到端加密和扫码配对。
6. 加固、诊断与全链路测试。

